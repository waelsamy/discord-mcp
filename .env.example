# Discord MCP Server Configuration

# Option 1: Token Authentication (Recommended)
# Run `uv run python get_token.py` to get your token
DISCORD_TOKEN=your_discord_token_here

# Option 2: Email/Password Authentication (Legacy)
# Only needed if DISCORD_TOKEN is not set
# DISCORD_EMAIL=your_email@example.com
# DISCORD_PASSWORD=your_password

# Other Settings
DISCORD_HEADLESS=true
DISCORD_GUILD_IDS=
MAX_MESSAGES_PER_CHANNEL=200
DEFAULT_HOURS_BACK=24